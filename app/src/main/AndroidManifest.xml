<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="personal.app.sharedpreferences_guia7">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SharedPreferences_Guia7">
        <activity android:name=".RespuestaActivity"
            android:label="Pista"></activity>
        <activity android:name=".PuntajeActivity"
            android:label="Puntuacion"/>
        <activity android:name=".JugarActivity"
            android:label="Adivina el numero"/>
        <activity android:name=".LoginActivity"
            android:label="Inicia sesion"/>
        <activity android:name=".MostrarDatosActivity"
            android:label="Mis datos personales"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>